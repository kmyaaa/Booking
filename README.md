# Booking
basic mutli-user booking web API.

- Open The Soultion.
- Apply the migrations.
- After applying the migration the database will be generated with seed data in trips and users tables.
- Run The Application.
- After Runing the application you will see the apis needed you can test it.
